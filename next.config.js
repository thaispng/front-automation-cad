/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // outras configurações conforme necessário
};

module.exports = nextConfig;
